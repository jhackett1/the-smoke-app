To ensure successful build:

* Remove all dev dependencies from /www/node_modules and .bin folder
* Minify all angular libs
* Change partials paths to ../www/ rather than ../
* Ensure use of ui-sref rather than href
